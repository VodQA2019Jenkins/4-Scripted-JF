stage 'Test & Archive Results'
node('Master') {

	checkout scm

    bat 'mvn clean compile package test'

    //step([$class: 'JUnitResultArchiver', 
      //    testResults: 'test-results/**/test-results.xml'])
          
}